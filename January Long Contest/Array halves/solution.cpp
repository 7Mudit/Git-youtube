#include<iostream>
using namespace std;

int main(){
    int arr[]={3,2,1,4};
    int n=sizeof(arr)/sizeof(arr[0]);
    bool ans=prev_permutation(arr, arr+n);
    for(auto x: arr)
        cout<<x<<" ";
        long long ans= long
}